% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_viz.R
\name{draw_correlation_heatmap_interactive}
\alias{draw_correlation_heatmap_interactive}
\title{draw_correlation_heatmap_interactive}
\usage{
draw_correlation_heatmap_interactive(
  data = NULL,
  conds = unique(stringr::str_split_fixed(colnames(data), "_", 2)[, 1]),
  correlation_method = "pearson",
  low_color = "#F7FCF5",
  mid_color = "#74C476",
  high_color = "#00441B",
  limits = NULL,
  title = NULL
)
}
\arguments{
\item{data}{a matrix of counts. This will be converted to log2+1}

\item{conds}{if NULL, shows all the conditions, else if character vector, shows only the required ones}

\item{correlation_method}{correlation method for the cor function. Can be pearson,
kendall or spearman.}

\item{low_color}{color for low correlation. Default : #F7FCF5}

\item{mid_color}{color for values in between high and low. Default : #74C476}

\item{high_color}{color for high correlation. Default : #00441B}

\item{limits}{vector with 2 elements, the lower and the higher limit of the
color scale. Default : the range of the observed correlations.}

\item{title}{plot title, or NULL for none.}
}
\description{
Interactive version of \code{draw_correlation_heatmap}, for narrow
boxes and high sample counts : hovering gives both sample names and the
correlation, so axis labels and cell values are not needed.
Returns a plotly object, which \code{download_plot_hd} cannot export : use
\code{draw_correlation_heatmap} for downloads.
}
\examples{
data("abiotic_stresses")
draw_correlation_heatmap_interactive(abiotic_stresses$normalized_counts)
}
